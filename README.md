# Demo App

First, run `pnpm install`. Then run locally or build.

## Running locally

```
pnpm start
```

The server will be running on port `2021`.

## Build

```
pnpm build
```

## Lint

```
pnpm lint
```

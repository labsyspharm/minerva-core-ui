import * as React from "react";
import styled from "styled-components";
import { Push, PopUpdate, Update } from "./EditButtons";

// Channel actions
export const PushChannel = Push;
export const PopUpdateChannel = PopUpdate;

// Group actions
export const PushGroup = Push;
export const PopUpdateGroup = PopUpdate;
export const UpdateGroup = Update;

// Waypoint actions
export const PushWaypoint = Push;
export const PopUpdateWaypoint = PopUpdate;
